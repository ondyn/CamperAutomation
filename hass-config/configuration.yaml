
# default_config replaced with explicit list: 'usb' and 'bluetooth' are excluded
# because aiousbwatcher/aiousbwatcher raises "no implementation for your platform
# ('posix') available" on Android/Termux (Python 3.13 sys.platform == 'android').
assist_pipeline:
cloud:
conversation:
energy:
file:
history:
homeassistant_alerts:
logbook:
media_source:
mobile_app:
my:
stream:
sun:
usage_prediction:
webhook:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
